#!/usr/bin/env python
# coding: utf-8

# In[1]:


data = "   삼성전자    "
data1 = data.strip()
print(data1)
#strip() 메서드를 사용하면 인자가 없을 경우 문자열 양쪽의 공백을 제거한다.
#인자가 있을 경우 인자가 된 문자열의 모든 조합을 제거한다.
#원본 문자열은 그대로 유지되고 공백이 제거된 새로운 문자열이 반환된다.

