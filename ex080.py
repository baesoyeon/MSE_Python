#!/usr/bin/env python
# coding: utf-8

# In[1]:


data = tuple(range(2, 100, 2))
print( data )
#range함수는 range(시작숫자, 종료숫자(n-1), 증가폭)의 형태이다.
#1부터 99까지의 정수이므로 시작숫자에 2, 종료숫자에 100(n-1이므로), 짝수이므로 증가폭을 2로 설정해 튜플을 생성하면 된다.

